<% semantic_form_for @infobit do |f| %>
  <%= f.error_messages %>
  <% f.inputs do %>
  	<%= f.input :name %>
  	<%= f.input :kind %>
  	<%= f.input :content %>
  	<%= f.input :infocollection, :include_blank => false, :as => :radio %> 
  <% end %>
  <%= f.buttons %> 
<% end %>
